{
  "name": "muhendis-berber",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter client run dev\" \"pnpm --filter server run dev\"",
    "build": "pnpm --filter client run build && pnpm --filter server run build",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
